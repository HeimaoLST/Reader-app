package com.reader.app.ui.theme

import androidx.compose.ui.graphics.Color

// Warm, Organic Palette (Claude-like)

// Light Theme
val WarmCream = Color(0xFFFBF9F6)
val SoftCharcoal = Color(0xFF2D2D2D)
val StoneGray = Color(0xFF5A5A5A)
val MutedTerracotta = Color(0xFFD9795F) 
val SageGreen = Color(0xFF8DA399)
val SlateBlue = Color(0xFF6B7C93)

// Dark Theme
val DarkCharcoalBg = Color(0xFF1E1E1E) // Slightly darker than SoftCharcoal for bg
val CreamText = Color(0xFFF0EBE5)
val MutedTerracottaDark = Color(0xFFE58A73)

// Semantic
val PrimaryLight = SoftCharcoal
val OnPrimaryLight = WarmCream
val BackgroundLight = WarmCream
val OnBackgroundLight = SoftCharcoal
val SurfaceLight = Color(0xFFF5F2ED) // Slightly darker cream for cards
val OnSurfaceLight = SoftCharcoal

val PrimaryDark = CreamText
val OnPrimaryDark = DarkCharcoalBg
val BackgroundDark = DarkCharcoalBg
val OnBackgroundDark = CreamText
val SurfaceDark = Color(0xFF2D2D2D)
val OnSurfaceDark = CreamText
